<Window x:Class="WledSimHubPlugin.CloneRuleToDeviceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Clone Rule to Device" Width="360" SizeToContent="Height"
        Background="#121212" Foreground="#F3F3F3" FontFamily="Segoe UI" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Clone Rule To" FontSize="16" FontWeight="Bold" Foreground="#F3F3F3" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="1" x:Name="RuleNameText" Foreground="#A0A0A0" FontSize="12" Margin="0,0,0,16"/>

        <TextBlock Grid.Row="2" Text="Select target device:" Foreground="#A0A0A0" FontSize="12" Margin="0,0,0,8"/>

        <ListBox Grid.Row="3" x:Name="DeviceList" Background="#1E1E1E" BorderBrush="#333" BorderThickness="1"
                 DisplayMemberPath="DeviceName" Margin="0,0,0,20" MinHeight="80">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Foreground" Value="#F3F3F3"/>
                    <Setter Property="Padding" Value="10,8"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="bd" Background="Transparent" BorderBrush="#252525" BorderThickness="0,0,0,1">
                                    <ContentPresenter Margin="{TemplateBinding Padding}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="bd" Property="Background" Value="#331DD383"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bd" Property="Background" Value="#1AFFFFFF"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Click="Cancel_Click" Background="Transparent" Foreground="#A0A0A0" BorderThickness="0" Padding="12,6" Cursor="Hand" Margin="0,0,16,0"/>
            <Button x:Name="CloneBtn" Content="Clone Rule" Click="Clone_Click" Background="#1DD383" Foreground="Black" FontWeight="Bold" BorderThickness="0" Padding="16,6" Cursor="Hand" IsEnabled="False">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="bg" Background="{TemplateBinding Background}" CornerRadius="4" Opacity="{TemplateBinding Opacity}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="bg" Property="Background" Value="#2BEA98"/>
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter TargetName="bg" Property="Opacity" Value="0.4"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>
