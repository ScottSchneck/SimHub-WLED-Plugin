<Window x:Class="WledSimHubPlugin.ProfilePropertiesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Profile Settings" Width="400" SizeToContent="Height"
        Background="#121212" Foreground="#F3F3F3" FontFamily="Segoe UI" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Profile Name" Foreground="#A0A0A0" Margin="0,0,0,4"/>
            <TextBox x:Name="NameBox" Background="#2A2A2A" Foreground="#F3F3F3" BorderBrush="#444" Padding="6" Text="{Binding ProfileName, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="Target Game" Foreground="#A0A0A0" Margin="0,0,0,4"/>
            <TextBox x:Name="GameBox" Background="#2A2A2A" Foreground="#F3F3F3" BorderBrush="#444" Padding="6" Text="{Binding Game, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,16">
            <TextBlock Text="Target Car (Optional)" Foreground="#A0A0A0" Margin="0,0,0,4"/>
            <TextBox x:Name="CarBox" Background="#2A2A2A" Foreground="#F3F3F3" BorderBrush="#444" Padding="6" Text="{Binding Car, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Click="Cancel_Click" Background="Transparent" Foreground="#A0A0A0" BorderThickness="0" Padding="12,6" Cursor="Hand" Margin="0,0,16,0"/>
            <Button Content="Save Profile" Click="Save_Click" Background="#1DD383" Foreground="Black" FontWeight="Bold" BorderThickness="0" Padding="16,6" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>